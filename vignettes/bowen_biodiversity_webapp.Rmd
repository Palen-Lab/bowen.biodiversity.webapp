---
title: "bowen_biodiversity_webapp"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{bowen_biodiversity_webapp}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(bowen.biodiversity.webapp)
```

# Running Zonation from R

```{r}


# Create features table from files available in data
features <- list.files(here::here("inst", "app", "rasters"),
                       recursive = T,
                       full.names = T)
layers_df <- data.frame(full_path = features)
layers_df$filename <- basename(layers_df$full_path)
layers_df$weights <- 1.0

zonation5(layers_df)

```

